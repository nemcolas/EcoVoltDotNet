@model IEnumerable<EcoVolt.Models.GsGeracaoEnergia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EnergiaGeradaKwh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fonte)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EnergiaGeradaKwh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fonte.IdFonte)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdGeracao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdGeracao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdGeracao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
