@model IEnumerable<EcoVolt.Models.GsCidade>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomCidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomCidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado.CodEstado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodCidade">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodCidade">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodCidade">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
