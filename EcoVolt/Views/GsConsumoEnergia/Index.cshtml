@model IEnumerable<EcoVolt.Models.GsConsumoEnergia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EnergiaConsumidaKwh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consumidor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EnergiaConsumidaKwh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Consumidor.IdConsumidor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdConsumo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdConsumo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdConsumo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
